# Minimal dependencies for platform integration (no Streamlit / no dashboard).
# Use: pip install -r requirements-integration.txt
# Then: pip install -e .   (to install the bureau_uw package from this repo)

joblib>=1.2
lxml>=4.9
numpy>=1.21
pandas>=1.3

# Optional only if using example_rest_api.py
fastapi>=0.100
uvicorn>=0.23
